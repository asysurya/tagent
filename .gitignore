# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# env & secrets — BYOK keys live in .tagent/config.json (never committed)
.env*
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# tagent runtime data (sessions, snapshots, local config with API keys)
demo-workspace/.tagent/*
!demo-workspace/.tagent/plugins/

# sandbox / harness internals — not part of the product
/.zscripts/
/Caddyfile
/examples/
/tests/
/skills/
/upload/
/download/
/db/
/worklog.md
/prompts/
/agent-ctx/
*.pid

# plugin runtime logs
.tagent/plugin-log.jsonl
mini-services/tagent-daemon/.tagent/
